import { id } from '../module.json';

/**
 * ID of this module, stored in package.json file.
 * Used to apply module-specific changes on Foundry, i.e.: CSS classes and Settings
 */
export const MODULE_ID = id;
